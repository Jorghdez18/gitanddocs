[project]
name = "2dam-dpl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Test Verif", email = "verif@test.local" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
2dam-dpl = "2dam_dpl:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "zensical>=0.0.65",
]
